Ты прислал сообщение



"Ссылка /edit/ не показывает содержимое без авторизации GitHub, но через Raw видно, что там всё ещё записана старая сборка:
text

-- NL: Inter (default)

и старый исходник с:
text

getenv
warning C4996
error C2220

Ты собираешь не последний архив. Исправление с _CRT_SECURE_NO_WARNINGS находится в последнем архиве:

Сделай так:

    Удали или переименуй старую папку:

text

C:\Users\1\Desktop\ArchLoader\NL

    Распакуй новый архив.

    Найди файл через PowerShell:

powershell

Get-ChildItem C:\Users\1\Desktop\ArchLoader -Filter CMakeLists.txt -Recurse

    Перейди именно в папку, где одновременно есть:

text

CMakeLists.txt
src
vendor
res

    Собери:

powershell

cd "C:\Users\1\Desktop\ArchLoader\NL"
cmake -S . -B build-final -G "Visual Studio 18 2026" -A x64 -T v145 -DNL_BUILD_APP=ON -DNL_BUILD_TESTS=OFF
cmake --build build-final --config Release

В правильной версии сообщение будет:
text

-- ArchLoader: Inter (default)

а не:
text

-- NL: Inter (default)
"




Если ты в нем что то изменял в файлах - откати
